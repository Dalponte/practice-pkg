export function visualNutsIntegerPrinter(endingNumber: number = 100) {
  const start = 1;
  if (endingNumber < 1)
    throw Error(`invalid endingNumber parameter cannot be bellow ${start}`);
  return true;
}
